import is from '../is'

export default {
  conditions: {

    // Evaluates to true if value is an array with unique values.
    $unique: function (d, q) {
      throw new Error('TODO')
      return false
    }

  }
}
